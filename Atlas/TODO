Expand SubTexture Rendering API (draw with no provided size)
Add ECS architecture (UUID)


<!-- ---------------------------------------------------------------------- -->

Clean up includes before commit
Convert raw pointers -- need to handle memory deallocation

UniformBufferLayout must be in order... can that be reworked?
Add Atlas-based logging window


xcrun -sdk macosx metal -o triangle.ir -c triangle.metal
xcrun -sdk macosx metal -o triangle.metallib triangle.ir

git ls-files | xargs wc -l
